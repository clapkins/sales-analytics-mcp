"""Пакет скиллов.

Каждый скилл — отдельный модуль в этом пакете с классом-наследником
``core.registry.BaseSkill``, помеченным декоратором
``@core.registry.register_skill``. ``core.registry.discover()`` находит
их автоматически через ``pkgutil`` — добавление нового скилла не
требует правок ни в этом файле, ни в ``core/``.
"""
